% A ConTeXt document [master document: crossCompiler.tex]

\section[title=Words]

\setCHeaderStream{private}
\startCHeader
extern void registerCrossCompilerWords(
  JoyLoLInterp* jInterp
);
\stopCHeader
\setCHeaderStream{public}

\startCCode
void registerCrossCompilerWords(
  JoyLoLInterp* jInterp
) {
  assert(jInterp);
  extendJoyLoL(jInterp, "addFragment", addFragmentAP);
}
\stopCCode
