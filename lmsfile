-- A Lua Make System file

lfs = require 'lfs'

testProbe = directorySeparator..'test'

-- for future use....
-- require 'lms.release'
-- release.targets{}

require 'lms.contextDoc'

createNewDocTarget('book', 'BookTargets')
contextDoc.targets{
  globalTargetVar = 'BookTargets',
  mainDoc         = 'protoJoyLoL.tex',
  docDir          = 'doc'
}

contextDoc.targets{
  mainDoc         = 'protoJoyLoL.tex',
  docDir          = 'doc'
}

if clearScreen then os.execute('reset') end

recurseTargets{}
