-- A Lua Make System file

lfs = require 'lfs'

testProbe = directorySeparator..'test'

-- for future use....
-- require 'lms.release'
-- release.targets{}

require 'lms.contextDoc'

createNewDocTarget('master', 'MasterTargets')
contextDoc.targets{
  globalTargetVar = 'MasterTargets',
  mainDoc         = 'joyLoL.tex',
  docDir          = 'doc'
}

createNewDocTarget('intro', 'IntroTargets')
contextDoc.targets{
  subDirs         = 'doc',
  globalTargetVar = 'IntroTargets',
  mainDoc         = 'joyLoLIntroOnly.tex',
  docDir          = 'doc'
}

if clearScreen then os.execute('reset') end

recurseTargets{}
