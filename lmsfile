-- A Lua Make System file

require 'lms.contextDoc'

contextDoc.targets{
  mainDoc  = 'computationalComplexity.tex',
  docFiles = {
    'preface.tex',
    'introduction.tex'
  },
  docDir    = 'doc',
}

